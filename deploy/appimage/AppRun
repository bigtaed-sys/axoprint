#!/bin/sh
# AppImage entry point: launch the bundled AxoPrint Linux client.
HERE="$(dirname "$(readlink -f "$0")")"
export DOTNET_BUNDLE_EXTRACT_BASE_DIR="${XDG_CACHE_HOME:-$HOME/.cache}/axoprint"
exec "$HERE/usr/bin/AxoPrint.LinuxClient" "$@"
